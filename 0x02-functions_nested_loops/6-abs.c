#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: number being tested
 * Return: Always
 */

int _abs(int)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
